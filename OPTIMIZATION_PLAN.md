# Asteroid 当前维护计划

> 更新时间：2026-08-24。本文件记录当前本地维护状态，不代替生产数据库操作手册。

## 当前结论

Asteroid 当前重点是维护本地验证门禁，使 Windows 与 CI/Linux 环境对同一份仓库得到一致结果。功能代码、领域测试和离线生产构建已有稳定基线；生产数据库是否应用迁移，不能根据本地文件或本地验证结果推断。

数据库与 RLS 的唯一操作指南是 `supabase/README.md`，迁移后的只读核验入口是 `supabase/verification.sql`。

## 生产安全边界

1. 现有生产项目不得重放 `0001–0007`。这些对象可能已经存在，但生产库不一定有对应的迁移历史记录。
2. 只有全新的空项目才按 `supabase/README.md` 从 `0001` 开始顺序执行迁移。
3. 任何现有生产项目的远程迁移都必须遵守对应工作包的 live audit、备份、fixed shadow、preflight/postflight 和独立确认要求。
4. 本地资产检查通过只说明脚本与迁移文件满足静态契约，不代表 Vercel 或 Supabase 已经配置、迁移或验收。
5. 不使用 `supabase-init.sql` 配置生产环境。

## 当前本地门禁

| 项目 | 状态 | 验收证据 |
| --- | --- | --- |
| WP2 migration checksum 跨平台一致性 | 已完成 | `0015` 在 LF/CRLF 工作区都映射到同一审阅哈希，同时保留其他字节的严格校验 |
| WP11 合集资产检查 | 已完成 | 校验缓存重构后的服务端公开缓存、详情客户端与阅读路径 |
| WP8–WP13 验证接入 | 已完成 | `verify:predeploy` 覆盖 AI 内容、审核、合集、任务中心、阅读助手与知识点快测 |
| 博客内容 skill | 已完成 | `verify:predeploy` 执行仓库内可复现的 skill 资产检查 |
| 本地浏览器截图 | 手动证据 | 截图目录被 Git 忽略，不作为 fresh clone 的发布门禁 |
| Lint、领域测试、离线生产构建 | 已通过 | `npm run verify:predeploy` 于 2026-08-24 完整通过，领域测试 106/106 |

## 2026-08-24 非 UI 优化复核

| 原候选项 | 当前结论 | 验收证据 |
| --- | --- | --- |
| 旧兼容逻辑与重复数据访问 | 已完成可安全退场部分 | `/debug` 改用 `getSummaries()`；移除无调用的 `getAll()`、`search()` 摘要别名 |
| 题库编辑器与阅读页题目行为 | 已统一领域规则 | 两个入口均使用 `normalizeProblem()` 与题目校验；新增回归测试固定选择题规范化、空选项和重复标签行为 |
| Markdown、LaTeX、题册打印与长文阅读 | 当前无已知发布阻塞项 | 新增最终 HTML 渲染测试，覆盖重复标题锚点、KaTeX 与危险链接；现有题册打印、历史难例与阅读器契约继续通过 |
| 缓存、交互和性能 | 无证据支持继续重构 | Next.js 16.2.10 离线生产构建通过；公开缓存、请求内去重与离线预加载边界保持现状 |
| Windows 本地门禁 | 已修复 | `test:run` 使用 `--test-isolation=none`，避免受管环境因测试文件子进程触发 `spawn EPERM`；106 条测试通过 |

以下兼容分支明确保留，不能仅凭本地代码判断删除：英语 `legacy/dual/shared` 模式、旧数据库字段 fallback、数学旧标签识别和旧客户端写入后的公开缓存失效入口。它们只能在生产迁移与存量数据核验完成后退场。

## 后续仅由新证据触发

1. 有生产迁移与存量数据核验结果后，再删除对应兼容分支。
2. 有真实 Web Vitals、慢查询、失败率或用户流程证据后，再调整缓存与交互实现。
3. 有具体 Markdown、LaTeX 或打印失败样本后，先加入最小回归样本，再修复渲染逻辑。

## 常用验证

```bash
npm run verify:predeploy
```

需要核验线上安全配置时，另按 `PRODUCTION_SECURITY_CHECKLIST.md` 和 `supabase/README.md` 操作；不得把本地通过描述成线上通过。
